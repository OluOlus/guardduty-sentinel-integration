{
  "name": "guardduty-sentinel-azure-function",
  "version": "1.0.0",
  "description": "Azure Function worker for GuardDuty to Sentinel integration",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/monitor-ingestion": "^1.0.0",
    "@azure/identity": "^4.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-kms": "^3.0.0"
  },
  "devDependencies": {
    "@azure/functions-core-tools": "^4.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}