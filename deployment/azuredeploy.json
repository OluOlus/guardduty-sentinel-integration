{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.5.0.0",
  "metadata": {
    "generatedBy": "scripts/sync_arm_template.py",
    "source": "kql/*.kql"
  },
  "parameters": {
    "workspaceName": {
      "type": "string",
      "metadata": {
        "description": "Existing Log Analytics workspace name"
      }
    },
    "environment": {
      "type": "string",
      "defaultValue": "prod",
      "allowedValues": [
        "dev",
        "test",
        "staging",
        "prod"
      ]
    }
  },
  "resources": [
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_Config')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_Config",
        "functionAlias": "AWSGuardDuty_Config",
        "functionParameters": "",
        "query": "// AWSGuardDuty_Config\n// Centralised configuration for all GuardDuty parsers.\n// Change settings here once; all downstream parsers read these values.\nlet AWSGuardDuty_Config = () {\n    datatable(\n        Setting: string,\n        Value: string,\n        Description: string,\n        DefaultValue: string\n    )[\n        // Core table configuration. Columns follow Microsoft's built-in\n        // AWSGuardDuty table contract; there is no EventData/Message raw column.\n        \"TableName\",             \"AWSGuardDuty\",              \"Informational: parser is compiled against this built-in table\",   \"AWSGuardDuty\",\n        \"InputContract\",         \"Microsoft-AWSGuardDuty\",    \"Microsoft built-in AWSGuardDuty table schema\",                    \"Microsoft-AWSGuardDuty\",\n\n        // Time and performance settings\n        \"DefaultLookback\",       \"7d\",                        \"Default time range when no lookback is specified\",                 \"7d\",\n        \"MaxLookback\",           \"90d\",                       \"Maximum allowed lookback period (guards against expensive queries)\",\"90d\",\n        \"QueryTimeout\",          \"300\",                       \"Query timeout in seconds\",                                         \"300\",\n\n        // Data quality thresholds\n        \"MinSeverityScore\",      \"0.0\",                       \"Minimum severity score to retain (0 = keep all)\",                  \"0.0\",\n        \"MaxSeverityScore\",      \"10.0\",                      \"Maximum expected severity score\",                                  \"10.0\",\n        \"RequiredFields\",        \"id,type,severity,accountId,region\", \"Required fields for a valid finding\",                      \"id,type,severity,accountId,region\",\n\n        // Parser metadata\n        \"ParserVersion\",         \"1.5.0\",                     \"Current parser package version\",                                   \"1.5.0\",\n        \"SchemaVersion\",         \"2.0\",                       \"Expected GuardDuty schemaVersion prefix\",                          \"2.0\",\n        \"LastUpdated\",           \"2026-07-23\",                \"Date of last parser update\",                                       \"2026-07-23\",\n\n        // Feature flags\n        \"EnableGeoEnrichment\",      \"true\",  \"Use geo_info_from_ip_address() to enrich remote IPs\",            \"true\",\n        \"EnableASIMNormalization\",  \"true\",  \"Enable ASIM schema normalization in ASIMNetworkSession parser\",   \"true\",\n        \"EnableDataValidation\",     \"true\",  \"Filter records that fail required-field validation\",              \"true\",\n        \"EnablePerformanceLogging\", \"false\", \"Emit per-query performance metadata (diagnostic use only)\",       \"false\"\n    ]\n};\nAWSGuardDuty_Config\n",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_Main')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_Main",
        "functionAlias": "AWSGuardDuty_Main",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_Main\n// Base parser for Microsoft's built-in AWSGuardDuty table contract.\n// Both the native S3 connector and the direct DCR adapter target this schema.\nlet AWSGuardDuty_Main = (lookback: timespan = timespan(null)) {\n    let config = AWSGuardDuty_Config();\n    let _defaultLookback = totimespan(toscalar(config | where Setting == \"DefaultLookback\" | project Value));\n    let _maxLookback = totimespan(toscalar(config | where Setting == \"MaxLookback\" | project Value));\n    let _lookback = iff(\n        isnull(lookback),\n        _defaultLookback,\n        iff(lookback > _maxLookback, _maxLookback, lookback)\n    );\n    let _enableValidation = tobool(toscalar(config | where Setting == \"EnableDataValidation\" | project Value));\n    let _minSeverity = todouble(toscalar(config | where Setting == \"MinSeverityScore\" | project Value));\n    let _maxSeverity = todouble(toscalar(config | where Setting == \"MaxSeverityScore\" | project Value));\n    let _parserVersion = toscalar(config | where Setting == \"ParserVersion\" | project Value);\n    //\n    AWSGuardDuty\n    | where TimeGenerated >= ago(_lookback)\n    // Lambda and EventBridge are at-least-once. Keep the newest copy of the\n    // stable GuardDuty finding ID before any downstream parser sees it.\n    | summarize arg_max(TimeGenerated, *) by Id\n    // Reconstruct the AWS API shape consumed by the specialist parsers.\n    | extend gd = bag_pack(\n        \"accountId\", AccountId,\n        \"arn\", Arn,\n        \"createdAt\", TimeCreated,\n        \"description\", Description,\n        \"id\", Id,\n        \"partition\", Partition,\n        \"region\", Region,\n        \"resource\", ResourceDetails,\n        \"schemaVersion\", SchemaVersion,\n        \"service\", ServiceDetails,\n        \"severity\", todouble(Severity),\n        \"title\", Title,\n        \"type\", ActivityType,\n        \"updatedAt\", TimeGenerated\n    )\n    | extend RawJson = tostring(gd)\n    | where not(_enableValidation) or (isnotempty(SchemaVersion) and SchemaVersion startswith \"2.\")\n    | extend\n        EventTime    = iff(isnotempty(tostring(TimeCreated)), todatetime(TimeCreated), TimeGenerated),\n        UpdatedTime  = TimeGenerated,\n        FindingId    = Id,\n        FindingType  = ActivityType,\n        Severity     = iff(\n            isnull(todouble(Severity)) or todouble(Severity) < _minSeverity or todouble(Severity) > _maxSeverity,\n            0.0,\n            todouble(Severity)\n        ),\n        AwsAccountId = AccountId,\n        AwsRegion    = Region,\n        DetectorId   = tostring(ServiceDetails.detectorId),\n        ServiceName  = tostring(ServiceDetails.serviceName),\n        ResourceType = tostring(ResourceDetails.resourceType),\n        ActionType   = tostring(ServiceDetails.action.actionType)\n    | extend DataQualityScore =\n        iff(isnotempty(FindingId),    20, 0) +\n        iff(isnotempty(FindingType),  20, 0) +\n        iff(Severity > 0,             20, 0) +\n        iff(isnotempty(AwsAccountId), 20, 0) +\n        iff(isnotempty(AwsRegion),    20, 0)\n    | where not(_enableValidation) or DataQualityScore >= 60\n    | extend SeverityLevel = case(\n        Severity >= 9.0, \"Critical\",\n        Severity >= 7.0, \"High\",\n        Severity >= 4.0, \"Medium\",\n        Severity >= 1.0, \"Low\",\n        Severity > 0,    \"Informational\",\n        \"Unknown\"\n    )\n    | extend\n        ParsingTimestamp = now(),\n        ParserVersion = _parserVersion,\n        RecordSource = \"AWSGuardDuty_Main\"\n    | project\n        TimeGenerated,\n        EventTime,\n        UpdatedTime,\n        ParsingTimestamp,\n        FindingId,\n        FindingType,\n        DetectorId,\n        Severity,\n        SeverityLevel,\n        Title,\n        Description,\n        AwsAccountId,\n        AwsRegion,\n        Partition,\n        ServiceName,\n        ResourceType,\n        ActionType,\n        DataQualityScore,\n        SchemaVersion,\n        ParserVersion,\n        RecordSource,\n        RawJson,\n        gd\n};\nAWSGuardDuty_Main\n",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_Network')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_Network",
        "functionAlias": "AWSGuardDuty_Network",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_Network\n// Network-focused parser for GuardDuty findings\n// Extracts network connection details with enhanced coverage and geographic enrichment\nlet AWSGuardDuty_Network = (lookback: timespan = timespan(null)) {\n    let config = AWSGuardDuty_Config();\n    let _enableGeoEnrichment = tobool(toscalar(config | where Setting == \"EnableGeoEnrichment\" | project Value));\n    //\n    AWSGuardDuty_Main(lookback)\n    // Enhanced filtering for network-related findings\n    | where ActionType == \"NETWORK_CONNECTION\" \n        or FindingType has_any(\"DNS\", \"Network\", \"Backdoor\", \"Trojan\", \"DDoS\", \"Port\", \"Tor\", \"Cryptocurrency\", \"Malware\")\n        or FindingType contains \"EC2/\"\n        or FindingType contains \"Behavior:\"\n    //\n    // Extract network connection details with error handling\n    | extend\n        ConnectionDirection = tostring(gd.service.action.networkConnectionAction.connectionDirection),\n        Protocol = tostring(gd.service.action.networkConnectionAction.protocol),\n        IsBlocked = tobool(gd.service.action.networkConnectionAction.blocked),\n        //\n        // Local port details\n        LocalPort = toint(gd.service.action.networkConnectionAction.localPortDetails.port),\n        LocalPortName = tostring(gd.service.action.networkConnectionAction.localPortDetails.portName),\n        //\n        // Remote IP and port details\n        RemoteIp = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.ipAddressV4),\n        RemoteIpV6 = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.ipAddressV6),\n        RemotePort = toint(gd.service.action.networkConnectionAction.remotePortDetails.port),\n        RemotePortName = tostring(gd.service.action.networkConnectionAction.remotePortDetails.portName),\n        //\n        // Geographic information from GuardDuty\n        RemoteCountry = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.country.countryName),\n        RemoteCountryCode = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.country.countryCode),\n        RemoteCity = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.city.cityName),\n        //\n        // Organization details\n        RemoteOrganization = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.organization.org),\n        RemoteIsp = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.organization.isp),\n        RemoteAsn = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.organization.asn),\n        RemoteAsnOrg = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.organization.asnOrg),\n        //\n        // Geolocation coordinates\n        RemoteLatitude = todouble(gd.service.action.networkConnectionAction.remoteIpDetails.geoLocation.lat),\n        RemoteLongitude = todouble(gd.service.action.networkConnectionAction.remoteIpDetails.geoLocation.lon)\n    //\n    // Extract instance context with enhanced details\n    | extend\n        InstanceId = tostring(gd.resource.instanceDetails.instanceId),\n        InstanceType = tostring(gd.resource.instanceDetails.instanceType),\n        InstanceState = tostring(gd.resource.instanceDetails.instanceState),\n        Platform = tostring(gd.resource.instanceDetails.platform),\n        LaunchTime = todatetime(gd.resource.instanceDetails.launchTime),\n        AvailabilityZone = tostring(gd.resource.instanceDetails.availabilityZone),\n        ImageId = tostring(gd.resource.instanceDetails.imageId),\n        ImageDescription = tostring(gd.resource.instanceDetails.imageDescription),\n        //\n        // Network interface details (first interface)\n        NetworkInterfaceId = tostring(gd.resource.instanceDetails.networkInterfaces[0].networkInterfaceId),\n        VpcId = tostring(gd.resource.instanceDetails.networkInterfaces[0].vpcId),\n        SubnetId = tostring(gd.resource.instanceDetails.networkInterfaces[0].subnetId),\n        PrivateIp = tostring(gd.resource.instanceDetails.networkInterfaces[0].privateIpAddress),\n        PrivateDnsName = tostring(gd.resource.instanceDetails.networkInterfaces[0].privateDnsName),\n        PublicIp = tostring(gd.resource.instanceDetails.networkInterfaces[0].publicIp),\n        PublicDnsName = tostring(gd.resource.instanceDetails.networkInterfaces[0].publicDnsName),\n        //\n        // Security group information\n        SecurityGroups = gd.resource.instanceDetails.networkInterfaces[0].securityGroups,\n        SecurityGroupIds = tostring(array_strcat(extract_all(@'\"groupId\":\"([^\"]+)\"', gd.resource.instanceDetails.networkInterfaces[0].securityGroups), \",\"))\n    //\n    // Enhanced geographic enrichment using Sentinel functions\n    | extend EnrichedGeoInfo = iff(\n        _enableGeoEnrichment and isnotempty(RemoteIp),\n        geo_info_from_ip_address(RemoteIp),\n        dynamic({})\n    )\n    | extend\n        EnrichedCountry = iff(_enableGeoEnrichment, tostring(EnrichedGeoInfo.country), \"\"),\n        EnrichedState = iff(_enableGeoEnrichment, tostring(EnrichedGeoInfo.state), \"\"),\n        EnrichedCity = iff(_enableGeoEnrichment, tostring(EnrichedGeoInfo.city), \"\"),\n        EnrichedLatitude = iff(_enableGeoEnrichment, todouble(EnrichedGeoInfo.latitude), double(null)),\n        EnrichedLongitude = iff(_enableGeoEnrichment, todouble(EnrichedGeoInfo.longitude), double(null))\n    //\n    // Determine traffic direction for ASIM compatibility\n    | extend TrafficDirection = case(\n        ConnectionDirection == \"INBOUND\", \"Inbound\",\n        ConnectionDirection == \"OUTBOUND\", \"Outbound\", \n        \"Unknown\"\n    )\n    //\n    // Categorize network threats\n    | extend ThreatCategory = case(\n        FindingType has \"Backdoor\", \"Backdoor\",\n        FindingType has \"Trojan\", \"Trojan\",\n        FindingType has \"DDoS\" or FindingType has \"DenialOfService\", \"DDoS\",\n        FindingType has \"Tor\", \"Anonymization\",\n        FindingType has \"Cryptocurrency\", \"Cryptocurrency Mining\",\n        FindingType has \"DNS\", \"DNS Abuse\",\n        FindingType has \"Port\", \"Port Scanning\",\n        \"Network Anomaly\"\n    )\n    //\n    // Calculate risk score based on multiple factors\n    | extend NetworkRiskScore = \n        iff(SeverityLevel == \"Critical\", 40, 0) +\n        iff(SeverityLevel == \"High\", 30, 0) +\n        iff(SeverityLevel == \"Medium\", 20, 0) +\n        iff(SeverityLevel == \"Low\", 10, 0) +\n        iff(ThreatCategory in (\"Backdoor\", \"Trojan\"), 20, 0) +\n        iff(RemoteCountry != \"United States\" and isnotempty(RemoteCountry), 10, 0) +\n        iff(IsBlocked == false, 10, 0)\n    //\n    // Project network-specific fields with enhanced schema\n    | project \n        // Core timestamps and identifiers\n        TimeGenerated,\n        EventTime,\n        FindingId,\n        FindingType,\n        ThreatCategory,\n        Severity,\n        SeverityLevel,\n        NetworkRiskScore,\n        Title,\n        Description,\n        AwsAccountId,\n        AwsRegion,\n        //\n        // Network connection details\n        ConnectionDirection,\n        TrafficDirection,\n        Protocol,\n        IsBlocked,\n        LocalPort,\n        LocalPortName,\n        RemoteIp,\n        RemoteIpV6,\n        RemotePort,\n        RemotePortName,\n        //\n        // Geographic information (GuardDuty native)\n        RemoteCountry,\n        RemoteCountryCode,\n        RemoteCity,\n        RemoteLatitude,\n        RemoteLongitude,\n        //\n        // Geographic information (Sentinel enriched)\n        EnrichedCountry,\n        EnrichedState,\n        EnrichedCity,\n        EnrichedLatitude,\n        EnrichedLongitude,\n        //\n        // Organization details\n        RemoteOrganization,\n        RemoteIsp,\n        RemoteAsn,\n        RemoteAsnOrg,\n        //\n        // AWS instance context\n        InstanceId,\n        InstanceType,\n        InstanceState,\n        Platform,\n        LaunchTime,\n        AvailabilityZone,\n        ImageId,\n        ImageDescription,\n        //\n        // Network infrastructure\n        NetworkInterfaceId,\n        VpcId,\n        SubnetId,\n        PrivateIp,\n        PrivateDnsName,\n        PublicIp,\n        PublicDnsName,\n        SecurityGroupIds,\n        //\n        // Metadata\n        ParserVersion,\n        RecordSource = \"AWSGuardDuty_Network\",\n        RawJson\n};\nAWSGuardDuty_Network",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_IAM')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_IAM",
        "functionAlias": "AWSGuardDuty_IAM",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_IAM\n// Enhanced IAM-focused parser for GuardDuty findings\n// Extracts API call details, identity context, and authentication information\nlet AWSGuardDuty_IAM = (lookback: timespan = timespan(null)) {\n    AWSGuardDuty_Main(lookback)\n    | where ActionType == \"AWS_API_CALL\" or FindingType has_any(\"IAM\", \"Stealth\", \"Policy\", \"Credential\", \"UnauthorizedAPI\", \"Persistence\", \"PrivilegeEscalation\")\n    //\n    // Extract comprehensive API call details with error handling\n    | extend\n        ApiName = coalesce(\n            tostring(gd.service.action.awsApiCallAction.api),\n            tostring(gd.service.action.dnsRequestAction.domain),\n            \"Unknown\"\n        ),\n        ServiceName_API = coalesce(\n            tostring(gd.service.action.awsApiCallAction.serviceName),\n            extract(@\"^([^\\.]+)\", 1, tostring(gd.service.action.awsApiCallAction.api)),\n            \"Unknown\"\n        ),\n        CallerType = coalesce(\n            tostring(gd.service.action.awsApiCallAction.callerType),\n            \"Unknown\"\n        ),\n        ErrorCode = tostring(gd.service.action.awsApiCallAction.errorCode),\n        UserAgent = coalesce(\n            tostring(gd.service.action.awsApiCallAction.userAgent),\n            \"Unknown\"\n        ),\n        // Enhanced remote IP extraction with fallbacks\n        RemoteIp_API = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteIpDetails.ipAddressV4),\n            tostring(gd.service.remoteIpDetails.ipAddressV4),\n            tostring(gd.service.action.networkConnectionAction.remoteIpDetails.ipAddressV4)\n        ),\n        RemoteCountry_API = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteIpDetails.country.countryName),\n            tostring(gd.service.remoteIpDetails.country.countryName)\n        ),\n        RemoteCity_API = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteIpDetails.city.cityName),\n            tostring(gd.service.remoteIpDetails.city.cityName)\n        ),\n        RemoteOrganization_API = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteIpDetails.organization.org),\n            tostring(gd.service.remoteIpDetails.organization.org)\n        ),\n        RemoteIsp_API = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteIpDetails.organization.isp),\n            tostring(gd.service.remoteIpDetails.organization.isp)\n        )\n    //\n    // Enhanced identity context extraction\n    | extend\n        // Account and user information\n        RemoteAccountId = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteAccountDetails.accountId),\n            tostring(gd.accountId)\n        ),\n        UserName = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteAccountDetails.affiliated),\n            tostring(gd.resource.accessKeyDetails.userName),\n            extract(@\"user/([^/]+)\", 1, tostring(gd.resource.accessKeyDetails.principalId))\n        ),\n        AccessKeyId = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteAccountDetails.accessKeyId),\n            tostring(gd.resource.accessKeyDetails.accessKeyId)\n        ),\n        PrincipalId = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteAccountDetails.principalId),\n            tostring(gd.resource.accessKeyDetails.principalId)\n        ),\n        SessionName = tostring(gd.service.action.awsApiCallAction.remoteAccountDetails.sessionName),\n        // Enhanced user type classification\n        UserType = case(\n            tostring(gd.resource.accessKeyDetails.userType) == \"IAMUser\", \"IAM User\",\n            tostring(gd.resource.accessKeyDetails.userType) == \"Root\", \"Root Account\",\n            tostring(gd.resource.accessKeyDetails.userType) == \"AssumedRole\", \"Assumed Role\",\n            isnotempty(tostring(gd.service.action.awsApiCallAction.remoteAccountDetails.sessionName)), \"Assumed Role\",\n            isnotempty(tostring(gd.resource.accessKeyDetails.accessKeyId)), \"IAM User\",\n            \"Unknown\"\n        )\n    //\n    // Enhanced resource context extraction\n    | extend\n        ResourceArn = coalesce(\n            tostring(gd.resource.resourceArn),\n            tostring(gd.resource.accessKeyDetails.accessKeyId)\n        ),\n        ResourceUserName = tostring(gd.resource.accessKeyDetails.userName),\n        ResourceUserType = tostring(gd.resource.accessKeyDetails.userType),\n        ResourceInstanceId = tostring(gd.resource.instanceDetails.instanceId),\n        ResourceInstanceType = tostring(gd.resource.instanceDetails.instanceType)\n    //\n    // Enhanced action result determination\n    | extend ActionResult = case(\n        isnotempty(ErrorCode) and ErrorCode != \"Success\", \"Failure\",\n        ErrorCode == \"Success\", \"Success\",\n        FindingType has \"Unauthorized\", \"Failure\",\n        FindingType has \"Stealth\", \"Success\", // Stealth activities are typically successful\n        isnotempty(ApiName), \"Success\",\n        \"Unknown\"\n    )\n    //\n    // Enhanced finding categorization with threat intelligence\n    | extend FindingCategory = case(\n        FindingType has_any(\"Stealth\", \"StealthyBehavior\"), \"Stealth Activity\",\n        FindingType has_any(\"Policy\", \"PolicyViolation\"), \"Policy Violation\",\n        FindingType has_any(\"Credential\", \"CredentialAccess\"), \"Credential Compromise\",\n        FindingType has_any(\"UnauthorizedAPI\", \"Unauthorized\"), \"Unauthorized Access\",\n        FindingType has_any(\"Persistence\", \"PersistentThreat\"), \"Persistence\",\n        FindingType has_any(\"PrivilegeEscalation\", \"Escalation\"), \"Privilege Escalation\",\n        FindingType has_any(\"Reconnaissance\", \"Recon\"), \"Reconnaissance\",\n        FindingType has_any(\"Discovery\", \"Enum\"), \"Discovery\",\n        \"IAM Activity\"\n    ),\n    // Risk scoring based on finding characteristics\n    RiskScore = case(\n        SeverityLevel == \"High\" and ActionResult == \"Success\", 90,\n        SeverityLevel == \"High\" and ActionResult == \"Failure\", 70,\n        SeverityLevel == \"Medium\" and ActionResult == \"Success\", 60,\n        SeverityLevel == \"Medium\" and ActionResult == \"Failure\", 40,\n        SeverityLevel == \"Low\", 20,\n        10\n    ),\n    // Authentication method inference\n    AuthenticationMethod = case(\n        isnotempty(SessionName), \"Assumed Role\",\n        isnotempty(AccessKeyId) and UserType == \"Root Account\", \"Root Access Key\",\n        isnotempty(AccessKeyId), \"Access Key\",\n        \"Unknown\"\n    ),\n    // Threat indicators\n    ThreatIndicators = case(\n        RemoteCountry_API in (\"China\", \"Russia\", \"North Korea\", \"Iran\"), \"High-Risk Geography\",\n        RemoteOrganization_API has_any(\"Tor\", \"VPN\", \"Proxy\"), \"Anonymization Service\",\n        UserAgent has_any(\"curl\", \"wget\", \"python\", \"boto\"), \"Programmatic Access\",\n        ApiName has_any(\"CreateUser\", \"AttachUserPolicy\", \"CreateRole\"), \"Privilege Operations\",\n        \"\"\n    )\n    //\n    // Data quality validation\n    | extend DataQuality = case(\n        isempty(FindingId), \"Missing FindingId\",\n        isempty(ApiName) and ActionType == \"AWS_API_CALL\", \"Missing API Name\",\n        isempty(RemoteIp_API) and ActionType == \"AWS_API_CALL\", \"Missing Remote IP\",\n        \"Good\"\n    )\n    //\n    // Filter out low-quality records if specified\n    | where DataQuality == \"Good\" or isempty(DataQuality)\n    //\n    // Project comprehensive IAM-specific fields\n    | project \n        TimeGenerated,\n        EventTime,\n        FindingId,\n        FindingType,\n        FindingCategory,\n        Severity,\n        SeverityLevel,\n        RiskScore,\n        Title,\n        Description,\n        AwsAccountId,\n        AwsRegion,\n        // API call details\n        ApiName,\n        ServiceName_API,\n        CallerType,\n        ActionResult,\n        ErrorCode,\n        UserAgent,\n        // Enhanced identity context\n        RemoteAccountId,\n        UserName,\n        UserType,\n        AccessKeyId,\n        PrincipalId,\n        SessionName,\n        AuthenticationMethod,\n        // Resource context\n        ResourceArn,\n        ResourceUserName,\n        ResourceUserType,\n        ResourceInstanceId,\n        ResourceInstanceType,\n        // Network context\n        RemoteIp_API,\n        RemoteCountry_API,\n        RemoteCity_API,\n        RemoteOrganization_API,\n        RemoteIsp_API,\n        // Threat intelligence\n        ThreatIndicators,\n        DataQuality,\n        RawJson\n};\nAWSGuardDuty_IAM",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_S3')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_S3",
        "functionAlias": "AWSGuardDuty_S3",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_S3\n// S3-focused parser for GuardDuty findings.\n// Covers Policy:S3/*, Stealth:S3/*, Discovery:S3/*, Impact:S3/*, Exfiltration:S3/* finding types.\n// Extracts bucket configuration, public access posture, encryption details, and the IAM caller context.\nlet AWSGuardDuty_S3 = (lookback: timespan = timespan(null)) {\n    AWSGuardDuty_Main(lookback)\n    | where ResourceType == \"S3Bucket\"\n        or FindingType has \"S3/\"\n        or FindingType has_any(\"Policy:S3\", \"Stealth:S3\", \"Discovery:S3\", \"Impact:S3\", \"Exfiltration:S3\",\n                               \"PenTest:S3\", \"Backdoor:S3\", \"Trojan:S3\")\n    // S3 bucket details (first bucket in the list — findings typically affect one bucket)\n    | extend _bucket = gd.resource.s3BucketDetails[0]\n    | extend\n        BucketName       = tostring(_bucket.name),\n        BucketArn        = tostring(_bucket.arn),\n        BucketType       = tostring(_bucket.type),           // e.g. \"Destination\"\n        BucketCreatedAt  = todatetime(_bucket.createdAt),\n        BucketOwnerId    = tostring(_bucket.owner.id),\n        BucketTags       = _bucket.tags\n    // Encryption configuration\n    | extend\n        EncryptionType   = tostring(_bucket.defaultServerSideEncryption.encryptionType),\n        KmsKeyArn        = tostring(_bucket.defaultServerSideEncryption.kmsMasterKeyArn)\n    // Bucket-level public access block\n    | extend _bpa = _bucket.publicAccess.permissionConfiguration.bucketLevelPermissions.blockPublicAccess\n    | extend\n        BlockPublicAcls      = tobool(_bpa.blockPublicAcls),\n        IgnorePublicAcls     = tobool(_bpa.ignorePublicAcls),\n        BlockPublicPolicy    = tobool(_bpa.blockPublicPolicy),\n        RestrictPublicBuckets= tobool(_bpa.restrictPublicBuckets)\n    // Account-level public access block\n    | extend _apa = _bucket.publicAccess.permissionConfiguration.accountLevelPermissions.blockPublicAccess\n    | extend\n        AccountBlockPublicAcls      = tobool(_apa.blockPublicAcls),\n        AccountIgnorePublicAcls     = tobool(_apa.ignorePublicAcls),\n        AccountBlockPublicPolicy    = tobool(_apa.blockPublicPolicy),\n        AccountRestrictPublicBuckets= tobool(_apa.restrictPublicBuckets)\n    // ACL-level public access flags (bucket level)\n    | extend _acl = _bucket.publicAccess.permissionConfiguration.bucketLevelPermissions.accessControlList\n    | extend\n        AclAllowsPublicRead  = tobool(_acl.allowsPublicReadAccess),\n        AclAllowsPublicWrite = tobool(_acl.allowsPublicWriteAccess)\n    // Bucket policy public access flags\n    | extend _policy = _bucket.publicAccess.permissionConfiguration.bucketLevelPermissions.bucketPolicy\n    | extend\n        PolicyAllowsPublicRead  = tobool(_policy.allowsPublicReadAccess),\n        PolicyAllowsPublicWrite = tobool(_policy.allowsPublicWriteAccess)\n    // Effective permission (NOT_PUBLIC / PUBLIC)\n    | extend EffectivePermission = tostring(_bucket.publicAccess.effectivePermission)\n    // IAM caller context when the finding has an access key (e.g. API-driven S3 access)\n    | extend\n        CallerUserName    = tostring(gd.resource.accessKeyDetails.userName),\n        CallerUserType    = tostring(gd.resource.accessKeyDetails.userType),\n        CallerAccessKeyId = tostring(gd.resource.accessKeyDetails.accessKeyId),\n        CallerPrincipalId = tostring(gd.resource.accessKeyDetails.principalId)\n    // Remote IP when available (e.g. Discovery or Exfiltration findings)\n    | extend\n        RemoteIp      = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteIpDetails.ipAddressV4),\n            tostring(gd.service.remoteIpDetails.ipAddressV4)\n        ),\n        RemoteCountry = coalesce(\n            tostring(gd.service.action.awsApiCallAction.remoteIpDetails.country.countryName),\n            tostring(gd.service.remoteIpDetails.country.countryName)\n        ),\n        ApiName       = tostring(gd.service.action.awsApiCallAction.api)\n    // S3 risk categorisation\n    | extend S3RiskCategory = case(\n        FindingType has_any(\"BucketPublicAccessGranted\", \"BucketBlockPublicAccessDisabled\"),   \"Public Exposure\",\n        FindingType has \"BucketEncryptionDisabled\",                                             \"Encryption Disabled\",\n        FindingType has \"ServerAccessLoggingDisabled\",                                          \"Audit Log Disabled\",\n        FindingType has \"Discovery\",                                                            \"Reconnaissance\",\n        FindingType has \"Exfiltration\",                                                         \"Data Exfiltration\",\n        FindingType has \"Impact\",                                                               \"Data Impact\",\n        FindingType has \"PenTest\",                                                              \"Penetration Testing\",\n        \"S3 Anomaly\"\n    )\n    // Exposure severity modifier: mark exposed buckets clearly\n    | extend IsPubliclyExposed = EffectivePermission == \"PUBLIC\"\n        or AclAllowsPublicRead == true\n        or AclAllowsPublicWrite == true\n        or PolicyAllowsPublicRead == true\n        or PolicyAllowsPublicWrite == true\n    // Risk score\n    | extend S3RiskScore =\n        iff(SeverityLevel == \"Critical\", 40, 0) +\n        iff(SeverityLevel == \"High\",     30, 0) +\n        iff(SeverityLevel == \"Medium\",   20, 0) +\n        iff(SeverityLevel == \"Low\",      10, 0) +\n        iff(IsPubliclyExposed,           30, 0) +\n        iff(EncryptionType == \"NONE\" or isempty(EncryptionType), 20, 0) +\n        iff(S3RiskCategory == \"Data Exfiltration\", 10, 0)\n    | project-away _bucket, _bpa, _apa, _acl, _policy\n    | project\n        // Core identifiers\n        TimeGenerated,\n        EventTime,\n        FindingId,\n        FindingType,\n        S3RiskCategory,\n        Severity,\n        SeverityLevel,\n        S3RiskScore,\n        Title,\n        Description,\n        AwsAccountId,\n        AwsRegion,\n        // Bucket identity\n        BucketName,\n        BucketArn,\n        BucketType,\n        BucketCreatedAt,\n        BucketOwnerId,\n        // Encryption\n        EncryptionType,\n        KmsKeyArn,\n        // Public access configuration\n        IsPubliclyExposed,\n        EffectivePermission,\n        BlockPublicAcls,\n        IgnorePublicAcls,\n        BlockPublicPolicy,\n        RestrictPublicBuckets,\n        AccountBlockPublicAcls,\n        AccountIgnorePublicAcls,\n        AccountBlockPublicPolicy,\n        AccountRestrictPublicBuckets,\n        AclAllowsPublicRead,\n        AclAllowsPublicWrite,\n        PolicyAllowsPublicRead,\n        PolicyAllowsPublicWrite,\n        // IAM caller context\n        CallerUserName,\n        CallerUserType,\n        CallerAccessKeyId,\n        CallerPrincipalId,\n        ApiName,\n        // Network context\n        RemoteIp,\n        RemoteCountry,\n        // Metadata\n        ParserVersion,\n        RecordSource = \"AWSGuardDuty_S3\",\n        RawJson\n};\nAWSGuardDuty_S3\n",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_EKS')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_EKS",
        "functionAlias": "AWSGuardDuty_EKS",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_EKS\n// EKS/Kubernetes-focused parser for GuardDuty findings.\n// Covers EKS audit log detections (*:Kubernetes/*) and EKS runtime detections (*:Runtime/*).\n// Extracts Kubernetes API call details, user context, pod/namespace context, and process information.\nlet AWSGuardDuty_EKS = (lookback: timespan = timespan(null)) {\n    AWSGuardDuty_Main(lookback)\n    | where FindingType has \"Kubernetes/\"\n        or FindingType has \"Runtime/\"\n        or ResourceType in (\"EKSCluster\", \"Container\")\n        or FindingType has_any(\n            \"CredentialAccess:Kubernetes\", \"DefenseEvasion:Kubernetes\", \"Discovery:Kubernetes\",\n            \"Execution:Kubernetes\", \"Impact:Kubernetes\", \"Persistence:Kubernetes\",\n            \"PrivilegeEscalation:Kubernetes\", \"Collection:Kubernetes\",\n            \"CredentialAccess:Runtime\", \"DefenseEvasion:Runtime\", \"Discovery:Runtime\",\n            \"Execution:Runtime\", \"Impact:Runtime\", \"Persistence:Runtime\",\n            \"PrivilegeEscalation:Runtime\", \"Exfiltration:Runtime\"\n        )\n    // EKS cluster details\n    | extend\n        ClusterName = tostring(gd.resource.eksClusterDetails.name),\n        ClusterArn  = tostring(gd.resource.eksClusterDetails.arn),\n        ClusterStatus = tostring(gd.resource.eksClusterDetails.status),\n        ClusterCreatedAt = todatetime(gd.resource.eksClusterDetails.createdAt),\n        ClusterTags = gd.resource.eksClusterDetails.tags\n    // Kubernetes audit log action details\n    | extend _k8s = gd.service.action.kubernetesApiCallAction\n    | extend\n        K8sApiName          = tostring(_k8s.requestUri),\n        K8sApiVerb          = tostring(_k8s.verb),\n        K8sSourceIp         = tostring(_k8s.sourceIPs[0]),\n        K8sUserAgent        = tostring(_k8s.userAgent),\n        K8sStatusCode       = toint(_k8s.statusCode),\n        K8sIsAdminCall      = tobool(tostring(_k8s.parameters) contains \"admin\")\n    // Kubernetes user context from additionalInfo\n    | extend\n        K8sUserName    = tostring(gd.service.additionalInfo.kubernetesUserName),\n        K8sUserGroups  = tostring(gd.service.additionalInfo.kubernetesUserGroups),\n        K8sUserType    = case(\n            tostring(gd.service.additionalInfo.kubernetesUserName) startswith \"system:serviceaccount:\", \"ServiceAccount\",\n            tostring(gd.service.additionalInfo.kubernetesUserName) startswith \"system:\", \"System\",\n            isnotempty(tostring(gd.service.additionalInfo.kubernetesUserName)), \"User\",\n            \"Unknown\"\n        )\n    // Kubernetes workload context (namespace, object kind/name)\n    | extend\n        K8sNamespace   = tostring(gd.service.additionalInfo.kubernetesWorkloadDetails.namespace),\n        K8sObjectKind  = tostring(gd.service.additionalInfo.kubernetesWorkloadDetails.type),\n        K8sObjectName  = tostring(gd.service.additionalInfo.kubernetesWorkloadDetails.name),\n        K8sContainerImage = tostring(gd.service.additionalInfo.kubernetesWorkloadDetails.containers[0].image),\n        K8sContainerName  = tostring(gd.service.additionalInfo.kubernetesWorkloadDetails.containers[0].name),\n        K8sIsPrivileged   = tobool(gd.service.additionalInfo.kubernetesWorkloadDetails.containers[0].securityContext.privileged)\n    // Runtime process context (EKS Runtime Monitoring findings)\n    | extend _rt = gd.service.runtimeDetails\n    | extend\n        RuntimeProcess     = tostring(_rt.process.name),\n        RuntimeProcessPath = tostring(_rt.process.executablePath),\n        RuntimeProcessPid  = toint(_rt.process.pid),\n        RuntimeProcessUser = tostring(_rt.process.user),\n        RuntimeProcessArgs = tostring(_rt.process.executableSha256),\n        RuntimeContext     = tostring(_rt.context.commandLineExample),\n        RuntimeFilePath    = tostring(_rt.context.targetProcess.executablePath)\n    // Network context for Kubernetes API calls\n    | extend\n        K8sRemoteIp      = tostring(gd.service.action.kubernetesApiCallAction.sourceIPs[0]),\n        K8sRemoteCountry = tostring(gd.service.action.networkConnectionAction.remoteIpDetails.country.countryName)\n    // Threat categorisation for EKS findings\n    | extend K8sThreatCategory = case(\n        FindingType has \"PrivilegeEscalation\",   \"Privilege Escalation\",\n        FindingType has \"CredentialAccess\",       \"Credential Access\",\n        FindingType has \"Persistence\",            \"Persistence\",\n        FindingType has \"DefenseEvasion\",         \"Defense Evasion\",\n        FindingType has \"Discovery\",              \"Discovery\",\n        FindingType has \"Execution\",              \"Execution\",\n        FindingType has \"Impact\",                 \"Impact\",\n        FindingType has \"Exfiltration\",           \"Exfiltration\",\n        FindingType has \"Collection\",             \"Collection\",\n        \"Kubernetes Anomaly\"\n    )\n    // Risk scoring\n    | extend K8sRiskScore =\n        iff(SeverityLevel == \"Critical\",    40, 0) +\n        iff(SeverityLevel == \"High\",        30, 0) +\n        iff(SeverityLevel == \"Medium\",      20, 0) +\n        iff(SeverityLevel == \"Low\",         10, 0) +\n        iff(K8sIsPrivileged == true,        20, 0) +\n        iff(K8sThreatCategory in (\"Privilege Escalation\", \"Credential Access\"), 15, 0) +\n        iff(K8sUserType == \"ServiceAccount\" and K8sThreatCategory != \"Discovery\", 10, 0)\n    | project-away _k8s, _rt\n    | project\n        // Core identifiers\n        TimeGenerated,\n        EventTime,\n        FindingId,\n        FindingType,\n        K8sThreatCategory,\n        Severity,\n        SeverityLevel,\n        K8sRiskScore,\n        Title,\n        Description,\n        AwsAccountId,\n        AwsRegion,\n        // EKS cluster context\n        ClusterName,\n        ClusterArn,\n        ClusterStatus,\n        ClusterCreatedAt,\n        // Kubernetes API call details\n        K8sApiName,\n        K8sApiVerb,\n        K8sStatusCode,\n        K8sSourceIp,\n        K8sUserAgent,\n        K8sIsAdminCall,\n        // Kubernetes identity\n        K8sUserName,\n        K8sUserType,\n        K8sUserGroups,\n        // Kubernetes workload context\n        K8sNamespace,\n        K8sObjectKind,\n        K8sObjectName,\n        K8sContainerImage,\n        K8sContainerName,\n        K8sIsPrivileged,\n        // Runtime process context (Runtime Monitoring findings)\n        RuntimeProcess,\n        RuntimeProcessPath,\n        RuntimeProcessPid,\n        RuntimeProcessUser,\n        RuntimeProcessArgs,\n        RuntimeContext,\n        RuntimeFilePath,\n        // Network context\n        K8sRemoteIp,\n        K8sRemoteCountry,\n        // Metadata\n        ParserVersion,\n        RecordSource = \"AWSGuardDuty_EKS\",\n        RawJson\n};\nAWSGuardDuty_EKS\n",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_Malware')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_Malware",
        "functionAlias": "AWSGuardDuty_Malware",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_Malware\n// Malware Protection-focused parser for GuardDuty findings.\n// Covers GuardDuty Malware Protection scan results for EC2 and ECS/EKS workloads:\n//   Execution:EC2/MaliciousFile, Execution:ECS/MaliciousFile, Execution:Kubernetes/MaliciousFile\n//   Execution:EC2/SuspiciousFile, Execution:ECS/SuspiciousFile\n// Extracts scan result details, threat names, file paths, and affected resource context.\nlet AWSGuardDuty_Malware = (lookback: timespan = timespan(null)) {\n    AWSGuardDuty_Main(lookback)\n    | where FindingType has_any(\n            \"Execution:EC2/MaliciousFile\",    \"Execution:EC2/SuspiciousFile\",\n            \"Execution:ECS/MaliciousFile\",    \"Execution:ECS/SuspiciousFile\",\n            \"Execution:Kubernetes/MaliciousFile\",\n            \"Backdoor:EC2/C&CActivity\",       \"Trojan:EC2/\",\n            \"Behavior:EC2/NetworkPortUnusual\"\n        )\n        or (ResourceType == \"Instance\" and FindingType has_any(\"Execution\", \"Backdoor\", \"Trojan\"))\n    // ── Malware scan result details ───────────────────────────────────────────\n    | extend _scan = gd.service.ebsVolumeScanDetails\n    | extend\n        ScanId              = tostring(_scan.scanId),\n        ScanType            = tostring(_scan.scanType),\n        ScanStartedAt       = todatetime(_scan.scanStartedAt),\n        ScanCompletedAt     = todatetime(_scan.scanCompletedAt),\n        TriggerFindingId    = tostring(_scan.triggerFindingId),\n        ScanResultStatus    = tostring(_scan.scanResultDetails.scanResultStatus)\n    // ── Threat file details (first detected threat) ───────────────────────────\n    | extend _threat = gd.service.ebsVolumeScanDetails.threats[0]\n    | extend\n        ThreatName          = tostring(_threat.name),\n        ThreatFilePath      = tostring(_threat.filePaths[0].filePath),\n        ThreatFileName      = tostring(_threat.filePaths[0].fileName),\n        ThreatFileSha256    = tostring(_threat.filePaths[0].fileSha256),\n        ThreatVolumeArn     = tostring(_threat.filePaths[0].volumeArn),\n        ThreatCount         = array_length(_threat.filePaths)\n    // ── All threat names (for multi-threat scans) ─────────────────────────────\n    | extend AllThreatNames = tostring(gd.service.ebsVolumeScanDetails.threats)\n    // ── EC2 instance context ──────────────────────────────────────────────────\n    | extend\n        InstanceId          = tostring(gd.resource.instanceDetails.instanceId),\n        InstanceType        = tostring(gd.resource.instanceDetails.instanceType),\n        InstanceState       = tostring(gd.resource.instanceDetails.instanceState),\n        Platform            = tostring(gd.resource.instanceDetails.platform),\n        ImageId             = tostring(gd.resource.instanceDetails.imageId),\n        AvailabilityZone    = tostring(gd.resource.instanceDetails.availabilityZone),\n        VpcId               = tostring(gd.resource.instanceDetails.networkInterfaces[0].vpcId),\n        PrivateIp           = tostring(gd.resource.instanceDetails.networkInterfaces[0].privateIpAddress),\n        PublicIp            = tostring(gd.resource.instanceDetails.networkInterfaces[0].publicIp)\n    // ── ECS container context (ECS Malware Protection findings) ──────────────\n    | extend _ecs = gd.resource.ecsClusterDetails\n    | extend\n        EcsClusterName      = tostring(_ecs.name),\n        EcsClusterArn       = tostring(_ecs.arn),\n        EcsTaskArn          = tostring(_ecs.taskDetails.arn),\n        EcsTaskFamily       = tostring(_ecs.taskDetails.taskCreatedAt),\n        EcsContainerImage   = tostring(_ecs.taskDetails.containers[0].image)\n    // ── Malware severity / category mapping ───────────────────────────────────\n    | extend MalwareCategory = case(\n        ThreatName has_any(\"EICAR\", \"Test\"),                    \"Test File\",\n        ThreatName has_any(\"Trojan\", \"RAT\"),                    \"Remote Access Trojan\",\n        ThreatName has_any(\"Ransom\", \"Ransomware\"),             \"Ransomware\",\n        ThreatName has_any(\"Coin\", \"Miner\", \"XMR\"),             \"Cryptominer\",\n        ThreatName has_any(\"Backdoor\", \"C2\", \"C&C\"),            \"Backdoor / C2\",\n        ThreatName has_any(\"Rootkit\"),                          \"Rootkit\",\n        ThreatName has_any(\"Worm\"),                             \"Worm\",\n        ThreatName has_any(\"Spyware\", \"Keylogger\"),             \"Spyware\",\n        ScanResultStatus == \"THREATS_FOUND\",                    \"Malware (unclassified)\",\n        \"Suspicious File\"\n    )\n    // ── Risk scoring ──────────────────────────────────────────────────────────\n    | extend MalwareRiskScore =\n        iff(SeverityLevel == \"Critical\",                       40, 0) +\n        iff(SeverityLevel == \"High\",                           30, 0) +\n        iff(SeverityLevel == \"Medium\",                         20, 0) +\n        iff(SeverityLevel == \"Low\",                            10, 0) +\n        iff(ScanResultStatus == \"THREATS_FOUND\",               25, 0) +\n        iff(MalwareCategory in (\"Ransomware\", \"Rootkit\"),      20, 0) +\n        iff(MalwareCategory == \"Remote Access Trojan\",         15, 0) +\n        iff(MalwareCategory == \"Backdoor / C2\",                15, 0) +\n        iff(isnotempty(ThreatFileSha256),                      10, 0)\n    | project-away _scan, _threat, _ecs\n    | project\n        // Core identifiers\n        TimeGenerated,\n        EventTime,\n        FindingId,\n        FindingType,\n        MalwareCategory,\n        Severity,\n        SeverityLevel,\n        MalwareRiskScore,\n        Title,\n        Description,\n        AwsAccountId,\n        AwsRegion,\n        // Scan result details\n        ScanId,\n        ScanType,\n        ScanStartedAt,\n        ScanCompletedAt,\n        ScanResultStatus,\n        TriggerFindingId,\n        // Threat file details\n        ThreatName,\n        ThreatFilePath,\n        ThreatFileName,\n        ThreatFileSha256,\n        ThreatVolumeArn,\n        ThreatCount,\n        AllThreatNames,\n        // EC2 instance context\n        InstanceId,\n        InstanceType,\n        InstanceState,\n        Platform,\n        ImageId,\n        AvailabilityZone,\n        VpcId,\n        PrivateIp,\n        PublicIp,\n        // ECS context\n        EcsClusterName,\n        EcsClusterArn,\n        EcsTaskArn,\n        EcsContainerImage,\n        // Metadata\n        ParserVersion,\n        RecordSource = \"AWSGuardDuty_Malware\",\n        RawJson\n};\nAWSGuardDuty_Malware\n",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_RDS')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_RDS",
        "functionAlias": "AWSGuardDuty_RDS",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_RDS\n// RDS/database-focused parser for GuardDuty findings.\n// Covers CredentialAccess:RDS/*, Discovery:RDS/*, Impact:RDS/* and\n// the GuardDuty RDS Protection login anomaly findings.\n// Extracts database instance context, login activity, and IAM caller details.\nlet AWSGuardDuty_RDS = (lookback: timespan = timespan(null)) {\n    AWSGuardDuty_Main(lookback)\n    | where ResourceType == \"RDSDBInstance\"\n        or FindingType has \"RDS/\"\n        or FindingType has_any(\n            \"CredentialAccess:RDS\", \"Discovery:RDS\", \"Impact:RDS\",\n            \"Exfiltration:RDS\", \"PenTest:RDS\"\n        )\n    // ── RDS instance details ─────────────────────────────────────────────────\n    | extend _rds = gd.resource.rdsDbInstanceDetails\n    | extend\n        DbInstanceId       = tostring(_rds.dbInstanceIdentifier),\n        DbClusterId        = tostring(_rds.dbClusterIdentifier),\n        DbEngine           = tostring(_rds.engine),\n        DbEngineVersion    = tostring(_rds.engineVersion),\n        DbInstanceClass    = tostring(_rds.dbInstanceClass),\n        DbInstancePort     = toint(_rds.dbInstancePort),\n        DbInstanceStatus   = tostring(_rds.dbInstanceStatus),\n        DbStorageEncrypted = tobool(_rds.storageEncrypted),\n        DbTags             = _rds.tags,\n        DbAvailabilityZone = tostring(_rds.availabilityZone),\n        DbMultiAz          = tobool(_rds.multiAz),\n        DbPubliclyAccessible = tobool(_rds.publiclyAccessible)\n    // ── RDS login activity (GuardDuty RDS Protection findings) ───────────────\n    | extend _login = gd.service.rdsLoginAttemptAction\n    | extend\n        LoginUser          = tostring(_login.loginAttributes[0].user),\n        LoginApplication   = tostring(_login.loginAttributes[0].application),\n        LoginResult        = tostring(_login.loginAttributes[0].successfulLoginBruteForcedAccounts),\n        LoginFailedCount   = toint(_login.loginAttributes[0].failedLoginAttempts),\n        LoginSuccessCount  = toint(_login.loginAttributes[0].successfulLoginAttempts),\n        RemoteIp_RDS       = tostring(_login.remoteIpDetails.ipAddressV4),\n        RemoteCountry_RDS  = tostring(_login.remoteIpDetails.country.countryName),\n        RemoteCity_RDS     = tostring(_login.remoteIpDetails.city.cityName),\n        RemoteOrg_RDS      = tostring(_login.remoteIpDetails.organization.org)\n    // ── IAM caller context (API-driven findings) ─────────────────────────────\n    | extend\n        CallerUserName     = tostring(gd.resource.accessKeyDetails.userName),\n        CallerUserType     = tostring(gd.resource.accessKeyDetails.userType),\n        CallerAccessKeyId  = tostring(gd.resource.accessKeyDetails.accessKeyId),\n        CallerPrincipalId  = tostring(gd.resource.accessKeyDetails.principalId),\n        ApiName            = tostring(gd.service.action.awsApiCallAction.api)\n    // ── Threat classification ─────────────────────────────────────────────────\n    | extend RdsThreatCategory = case(\n        FindingType has \"CredentialAccess\",                     \"Credential Access\",\n        FindingType has \"BruteForce\",                           \"Brute Force\",\n        FindingType has \"AnomalousBehavior\",                    \"Anomalous Behaviour\",\n        FindingType has \"Discovery\",                            \"Reconnaissance\",\n        FindingType has \"Exfiltration\",                         \"Data Exfiltration\",\n        FindingType has \"Impact\",                               \"Data Impact\",\n        FindingType has \"PenTest\",                              \"Penetration Testing\",\n        \"RDS Anomaly\"\n    )\n    // ── Risk scoring ──────────────────────────────────────────────────────────\n    | extend RdsRiskScore =\n        iff(SeverityLevel == \"Critical\",         40, 0) +\n        iff(SeverityLevel == \"High\",             30, 0) +\n        iff(SeverityLevel == \"Medium\",           20, 0) +\n        iff(SeverityLevel == \"Low\",              10, 0) +\n        iff(DbPubliclyAccessible == true,        20, 0) +\n        iff(DbStorageEncrypted == false,         15, 0) +\n        iff(RdsThreatCategory == \"Brute Force\",  15, 0) +\n        iff(RdsThreatCategory == \"Data Exfiltration\", 10, 0)\n    | project-away _rds, _login\n    | project\n        // Core identifiers\n        TimeGenerated,\n        EventTime,\n        FindingId,\n        FindingType,\n        RdsThreatCategory,\n        Severity,\n        SeverityLevel,\n        RdsRiskScore,\n        Title,\n        Description,\n        AwsAccountId,\n        AwsRegion,\n        // RDS instance context\n        DbInstanceId,\n        DbClusterId,\n        DbEngine,\n        DbEngineVersion,\n        DbInstanceClass,\n        DbInstancePort,\n        DbInstanceStatus,\n        DbStorageEncrypted,\n        DbPubliclyAccessible,\n        DbMultiAz,\n        DbAvailabilityZone,\n        // Login activity (RDS Protection findings)\n        LoginUser,\n        LoginApplication,\n        LoginFailedCount,\n        LoginSuccessCount,\n        // IAM caller context\n        CallerUserName,\n        CallerUserType,\n        CallerAccessKeyId,\n        CallerPrincipalId,\n        ApiName,\n        // Network context\n        RemoteIp_RDS,\n        RemoteCountry_RDS,\n        RemoteCity_RDS,\n        RemoteOrg_RDS,\n        // Metadata\n        ParserVersion,\n        RecordSource = \"AWSGuardDuty_RDS\",\n        RawJson\n};\nAWSGuardDuty_RDS\n",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_ASIMNetworkSession')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_ASIMNetworkSession",
        "functionAlias": "AWSGuardDuty_ASIMNetworkSession",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_ASIMNetworkSession\n// Maps GuardDuty network findings to ASIM Network Session schema v0.2.7.\n// All column references are sourced from AWSGuardDuty_Network() output schema only.\nlet AWSGuardDuty_ASIMNetworkSession = (lookback: timespan = timespan(null)) {\n    AWSGuardDuty_Network(lookback)\n    | where isnotempty(RemoteIp)\n    // ASIM Network Session schema fields (v0.2.7)\n    | extend\n        // Standard event metadata\n        EventType              = \"NetworkSession\",\n        EventSchemaVersion     = \"0.2.7\",\n        EventVendor            = \"AWS\",\n        EventProduct           = \"GuardDuty\",\n        EventProductVersion    = \"1.0\",\n        EventCount             = int(1),\n        EventStartTime         = EventTime,\n        EventEndTime           = EventTime,\n        EventResult            = \"Success\",      // GuardDuty detects allowed connections\n        EventSeverity          = SeverityLevel,\n        EventOriginalSeverity  = tostring(Severity),\n        EventMessage           = Title,\n        EventOriginalType      = FindingType,\n        EventOriginalUid       = FindingId,\n        EventUid               = strcat(\"guardduty-\", FindingId),\n        // Source fields (AWS resource side)\n        SrcIpAddr    = coalesce(PrivateIp, PublicIp, \"\"),\n        SrcPortNumber = LocalPort,\n        SrcHostname  = coalesce(InstanceId, \"\"),\n        SrcDvcId     = coalesce(InstanceId, \"\"),\n        SrcDvcIdType = iff(isnotempty(InstanceId), \"Other\", \"\"),\n        SrcGeoCountry = AwsRegion,\n        SrcGeoRegion  = AwsRegion,\n        SrcDomain     = coalesce(VpcId, \"\"),\n        SrcDomainType = iff(isnotempty(VpcId), \"Other\", \"\"),\n        // Destination fields (remote endpoint)\n        DstIpAddr     = RemoteIp,\n        DstPortNumber = RemotePort,\n        DstGeoCountry = coalesce(RemoteCountry, \"\"),\n        DstGeoCity    = coalesce(RemoteCity, \"\"),\n        DstGeoRegion  = coalesce(RemoteCountry, \"\"),\n        DstHostname   = coalesce(PublicDnsName, \"\"),\n        // Network fields\n        NetworkProtocol = case(\n            Protocol == \"TCP\",  \"TCP\",\n            Protocol == \"UDP\",  \"UDP\",\n            Protocol == \"ICMP\", \"ICMP\",\n            toupper(Protocol)\n        ),\n        NetworkDirection  = TrafficDirection,\n        NetworkSessionId  = FindingId,\n        NetworkBytes      = int(0),    // GuardDuty does not provide byte counts\n        NetworkPackets    = int(0),    // GuardDuty does not provide packet counts\n        NetworkApplicationProtocol = case(\n            RemotePort == 80,   \"HTTP\",\n            RemotePort == 443,  \"HTTPS\",\n            RemotePort == 53,   \"DNS\",\n            RemotePort == 22,   \"SSH\",\n            RemotePort == 3389, \"RDP\",\n            RemotePort == 25,   \"SMTP\",\n            RemotePort == 21,   \"FTP\",\n            \"\"\n        ),\n        // Device fields (the EC2 instance or resource that triggered the finding)\n        DvcId        = coalesce(InstanceId, \"\"),\n        DvcIdType    = iff(isnotempty(InstanceId), \"Other\", \"\"),\n        DvcHostname  = coalesce(InstanceId, \"\"),\n        DvcOs        = case(\n            Platform == \"windows\", \"Windows\",\n            Platform == \"linux\",   \"Linux\",\n            isnotempty(Platform),  Platform,\n            \"Linux\"\n        ),\n        DvcOsVersion = \"\",\n        DvcAction    = \"Allow\",\n        DvcInterface = coalesce(NetworkInterfaceId, \"\"),\n        // Threat fields\n        ThreatName               = FindingType,\n        ThreatRiskLevel          = case(\n            SeverityLevel == \"Critical\",      95,\n            SeverityLevel == \"High\",          80,\n            SeverityLevel == \"Medium\",        50,\n            SeverityLevel == \"Low\",           20,\n            SeverityLevel == \"Informational\", 10,\n            toint(Severity * 10)\n        ),\n        ThreatOriginalRiskLevel  = tostring(Severity),\n        ThreatField              = iff(isnotempty(RemoteIp), \"DstIpAddr\", \"\"),\n        ThreatIpAddr             = RemoteIp,\n        ThreatIsActive           = true,\n        ThreatFirstReportedTime  = EventTime,\n        ThreatLastReportedTime   = EventTime,\n        // Rule fields\n        RuleName   = FindingType,\n        RuleNumber = int(0),\n        RuleAction = \"Alert\",\n        // AWS-specific context packed for ASIM AdditionalFields\n        AdditionalFields = bag_pack(\n            \"AwsAccountId\",      AwsAccountId,\n            \"AwsRegion\",         AwsRegion,\n            \"VpcId\",             VpcId,\n            \"SubnetId\",          SubnetId,\n            \"InstanceId\",        InstanceId,\n            \"InstanceType\",      InstanceType,\n            \"SecurityGroupIds\",  SecurityGroupIds,\n            \"NetworkInterfaceId\",NetworkInterfaceId,\n            \"RemoteOrganization\",RemoteOrganization,\n            \"RemoteIsp\",         RemoteIsp,\n            \"RemoteAsn\",         RemoteAsn,\n            \"GuardDutyFindingId\",FindingId,\n            \"Platform\",          Platform,\n            \"ImageId\",           ImageId,\n            \"LaunchTime\",        LaunchTime\n        )\n    // ASIM compliance check — must have both IP addresses and a protocol\n    | extend ASIMCompliance = case(\n        isempty(SrcIpAddr) and isempty(DstIpAddr), \"Missing IP addresses\",\n        isempty(NetworkProtocol),                  \"Missing protocol\",\n        isempty(tostring(EventTime)),              \"Missing timestamp\",\n        \"Compliant\"\n    )\n    | where ASIMCompliance == \"Compliant\"\n    | project\n        // ASIM event metadata\n        TimeGenerated,\n        EventType,\n        EventSchemaVersion,\n        EventVendor,\n        EventProduct,\n        EventProductVersion,\n        EventCount,\n        EventStartTime,\n        EventEndTime,\n        EventResult,\n        EventSeverity,\n        EventOriginalSeverity,\n        EventMessage,\n        EventOriginalType,\n        EventOriginalUid,\n        EventUid,\n        // Source\n        SrcIpAddr,\n        SrcPortNumber,\n        SrcHostname,\n        SrcDvcId,\n        SrcDvcIdType,\n        SrcGeoCountry,\n        SrcGeoRegion,\n        SrcDomain,\n        SrcDomainType,\n        // Destination\n        DstIpAddr,\n        DstPortNumber,\n        DstGeoCountry,\n        DstGeoCity,\n        DstGeoRegion,\n        DstHostname,\n        // Network\n        NetworkProtocol,\n        NetworkDirection,\n        NetworkSessionId,\n        NetworkBytes,\n        NetworkPackets,\n        NetworkApplicationProtocol,\n        // Device\n        DvcId,\n        DvcIdType,\n        DvcHostname,\n        DvcOs,\n        DvcOsVersion,\n        DvcAction,\n        DvcInterface,\n        // Threat\n        ThreatName,\n        ThreatCategory,\n        ThreatRiskLevel,\n        ThreatOriginalRiskLevel,\n        ThreatField,\n        ThreatIpAddr,\n        ThreatIsActive,\n        ThreatFirstReportedTime,\n        ThreatLastReportedTime,\n        // Rule\n        RuleName,\n        RuleNumber,\n        RuleAction,\n        // Additional context\n        AdditionalFields,\n        ASIMCompliance,\n        RawJson\n};\nAWSGuardDuty_ASIMNetworkSession\n",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    },
    {
      "type": "Microsoft.OperationalInsights/workspaces/savedSearches",
      "apiVersion": "2020-08-01",
      "name": "[concat(parameters('workspaceName'), '/AWSGuardDuty_Schema')]",
      "properties": {
        "category": "GuardDuty",
        "displayName": "AWSGuardDuty_Schema",
        "functionAlias": "AWSGuardDuty_Schema",
        "functionParameters": "lookback:timespan=timespan(null)",
        "query": "// AWSGuardDuty_Schema\n// Data-quality validation against Microsoft's built-in AWSGuardDuty columns.\nlet AWSGuardDuty_Schema = (lookback: timespan = timespan(null)) {\n    let config = AWSGuardDuty_Config();\n    let _defaultLookback = totimespan(toscalar(config | where Setting == \"DefaultLookback\" | project Value));\n    let _maxLookback = totimespan(toscalar(config | where Setting == \"MaxLookback\" | project Value));\n    let _lookback = iff(isnull(lookback), _defaultLookback, iff(lookback > _maxLookback, _maxLookback, lookback));\n    //\n    AWSGuardDuty\n    | where TimeGenerated >= ago(_lookback)\n    | extend\n        HasFindingId = isnotempty(Id),\n        HasFindingType = isnotempty(ActivityType),\n        HasSeverity = isnotnull(Severity) and Severity between (0 .. 10),\n        HasAccountId = isnotempty(AccountId),\n        HasRegion = isnotempty(Region),\n        HasTimestamp = isnotnull(TimeCreated) and isnotnull(TimeGenerated),\n        HasResource = isnotnull(ResourceDetails) and ResourceDetails != dynamic({}),\n        HasService = isnotnull(ServiceDetails) and ServiceDetails != dynamic({})\n    | extend QualityCategory = case(\n        not(HasFindingId), \"Missing FindingId\",\n        not(HasFindingType), \"Missing FindingType\",\n        not(HasSeverity), \"Invalid Severity\",\n        not(HasAccountId), \"Missing AccountId\",\n        not(HasRegion), \"Missing Region\",\n        not(HasTimestamp), \"Missing Timestamp\",\n        not(HasResource), \"Missing ResourceDetails\",\n        not(HasService), \"Missing ServiceDetails\",\n        isempty(SchemaVersion), \"Missing Schema Version\",\n        not(SchemaVersion startswith \"2.\"), \"Unsupported Schema Version\",\n        \"Valid\"\n    )\n    | project\n        TimeGenerated,\n        Id,\n        ActivityType,\n        QualityCategory,\n        SchemaVersion,\n        AccountId,\n        Region\n};\nAWSGuardDuty_Schema\n",
        "tags": {
          "Environment": "[parameters('environment')]",
          "Version": "1.5.0"
        }
      }
    }
  ],
  "outputs": {
    "deployedFunctions": {
      "type": "array",
      "value": [
        "AWSGuardDuty_Config",
        "AWSGuardDuty_Main",
        "AWSGuardDuty_Network",
        "AWSGuardDuty_IAM",
        "AWSGuardDuty_S3",
        "AWSGuardDuty_EKS",
        "AWSGuardDuty_Malware",
        "AWSGuardDuty_RDS",
        "AWSGuardDuty_ASIMNetworkSession",
        "AWSGuardDuty_Schema"
      ]
    }
  }
}
